{
  "name": "@juancode/wire-conformance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Versioned wire-protocol spec plus the conformance suite every juancode core (Swift, Rust) must pass.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:conformance": "vitest run --config vitest.conformance.config.ts",
    "parity": "tsx src/parity-cli.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
